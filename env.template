PRIVATE_KEY=
CLOB_HOST=https://clob.polymarket.com
CHAIN_ID=137
GAMMA_HOST=https://gamma-api.polymarket.com
# Per-outcome prices (if unset, fall back to TARGET_PRICE/SELL_PRICE below)
TARGET_PRICE_UP=0.45
SELL_PRICE_UP=0.55
TARGET_PRICE_DOWN=0.45
SELL_PRICE_DOWN=0.55
# Optional fallback when per-outcome vars are not set
# TARGET_PRICE=0.45
# SELL_PRICE=0.55
# Shares (tokens) per side; USD per side = ORDER_AMOUNT_TOKEN × TARGET_PRICE_UP or TARGET_PRICE_DOWN (min 5)
ORDER_AMOUNT_TOKEN=5
CHECK_INTERVAL=10000
# Delay (ms) after a BUY fill before placing SELL so CLOB can credit outcome tokens (default 10000)
SELL_DELAY_MS=10000
# Min seconds left to enter a market; default 20 = enter/sell even 20s before close (set e.g. 240 for 4 min)
# MIN_SECONDS_TO_ENTER=20
# Skip markets that expire in less than this many seconds (default 180 = 3 min)
# MIN_SECONDS_BEFORE_EXPIRY=180
# When this many seconds left before market end, cancel resting SELL and place aggressive exit (default 20)
# EXIT_BEFORE_CLOSE_SECONDS=20
# Price for emergency exit SELL when market is about to close (default 0.40)
# AGGRESSIVE_EXIT_PRICE=0.4

# Trading mode: continuous (loop forever) or once (trade only one market duration, then exit)
TRADING_MODE=continuous
# TRADING_MODE=once

# Optional: append all trading logs to a file (e.g. logs/trading.log)
# LOG_FILE=logs/trading.log

FUNDER_ADDRESS=
POLY_API_KEY=
POLY_SECRET=
POLY_PASSPHRASE=